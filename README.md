```markdown
# 🗓️ Система Бронирования

Профессиональная система для управления бронированиями с веб-интерфейсом. Программа позволяет удобно управлять клиентами, бронированиями и генерировать отчеты.

## ✨ Возможности

- **📅 Управление бронированиями** - создание, редактирование, отмена бронирований
- **👥 Управление клиентами** - база данных клиентов с историей бронирований
- **📊 Отчеты и аналитика** - статистика по бронированиям и доходам
- **⚙️ Настройки системы** - гибкая настройка параметров работы
- **🔐 Аутентификация** - безопасный доступ к системе
- **🌐 Веб-интерфейс** - современный и интуитивно понятный интерфейс

## 🚀 Быстрый старт

### Установка готовой версии (для конечных пользователей)

1. Скачайте установочный файл `BookingSystemSetup.exe`
2. Запустите установщик и следуйте инструкциям
3. После установки программа будет доступна:
   - В меню Пуск → Booking System
   - На рабочем столе (если выбран ярлык)
   - В папке установки (по умолчанию `C:\Program Files\Booking System`)

4. При первом запуске автоматически:
   - Создастся база данных
   - Откроется браузер с веб-интерфейсом
   - Программа запустит локальный сервер

### Системные требования

- **ОС:** Windows 10/11 (64-bit)
- **Память:** 512 МБ ОЗУ
- **Место на диске:** 100 МБ свободного места
- **Браузер:** Chrome, Firefox, Edge (последние версии)
- **Дополнительно:** Разрешение на запуск в брандмауэре

## 🛠️ Для разработчиков

### Установка и запуск из исходного кода

1. **Клонируйте репозиторий**
   ```bash
   git clone <repository-url>
   cd booking_system
   ```

2. **Создайте виртуальное окружение**
   ```bash
   python -m venv .venv
   .venv\Scripts\activate  # Windows
   ```

3. **Установите зависимости**
   ```bash
   pip install -r requirements.txt
   ```

4. **Запустите приложение**
   ```bash
   cd backend
   python run.py
   ```

5. **Откройте в браузере**
   ```
   http://127.0.0.1:5000
   ```

### Сборка в EXE файл

1. **Активируйте виртуальное окружение**
   ```bash
   .venv\Scripts\activate
   ```

2. **Установите PyInstaller**
   ```bash
   pip install pyinstaller
   ```

3. **Выполните сборку**
   ```bash
   pyinstaller build.spec
   ```

4. **Создайте установщик** (требуется Inno Setup)
   ```bash
   iscc installer.iss
   ```

## 📁 Структура проекта

```
booking_system/
├── backend/                 # Серверная часть
│   ├── app/
│   │   ├── routes/         # Маршруты API
│   │   ├── utils/          # Вспомогательные функции
│   │   ├── middleware.py   # Промежуточное ПО
│   │   └── models.py       # Модели данных
│   └── run.py              # Точка входа сервера
├── frontend/               # Клиентская часть
│   ├── css/               # Стили
│   ├── js/                # JavaScript
│   │   └── modules/       # Модули JS
│   └── index.html         # Главная страница
├── dist/                  # Собранные файлы (после сборки)
└── build/                 # Временные файлы сборки
```

## 🔧 API Endpoints

- `GET /` - Главная страница
- `POST /api/auth/login` - Авторизация
- `GET /api/bookings` - Список бронирований
- `POST /api/bookings` - Создание бронирования
- `PUT /api/bookings/<id>` - Обновление бронирования
- `GET /api/clients` - Список клиентов
- `GET /api/reports` - Отчеты

## 🗃️ База данных

Программа использует SQLite базу данных. Файл базы создается автоматически при первом запуске:

- **В разработке:** `backend/instance/booking.db`
- **В EXE версии:** `%LOCALAPPDATA%\BookingSystem\booking.db`

## ❗ Решение проблем

### Программа не запускается

1. **Проверьте антивирус** - некоторые антивирусы блокируют EXE файлы
2. **Запустите от имени администратора**
3. **Проверьте логи:** `logs/booking_system.log` рядом с EXE файлом

### Не открывается веб-интерфейс

1. **Проверьте браузер** - убедитесь, что браузер не блокирует локальные адреса
2. **Проверьте порт 5000** - убедитесь, что порт не занят другой программой
3. **Попробуйте адрес:** `http://localhost:5000`

### Ошибка базы данных

1. **Проверьте права доступа** к папке с программой
2. **Удалите файл базы данных** для пересоздания (резервные копии будут потеряны)

## 📝 Лицензия

[Укажите вашу лицензию]

## 🤝 Поддержка

При возникновении проблем:

1. Проверьте файл логов `logs/booking_system.log`
2. Убедитесь, что система соответствует требованиям
3. Проверьте, что все необходимые порты свободны

Для обратной связи: [ваши контакты]

---
**Версия:** 1.0  
**Последнее обновление:** 2025
```

## Дополнительные файлы, которые можно добавить:

### 1. `LICENSE` файл
Добавьте файл с лицензией вашего проекта.

### 2. `CHANGELOG.md`
```markdown
# История изменений

## [1.0] - 2025-01-01
### Добавлено
- Первоначальный выпуск системы бронирования
- Основные функции управления бронированиями и клиентами
- Веб-интерфейс
- Сборка в EXE
```

### 3. `.gitignore` (если используете Git)
```
__pycache__/
*.pyc
*.pyo
*.pyd
.Python
env/
venv/
.venv/
*.db
*.log
dist/
build/
*.spec
.DS_Store
```

Этот README.md полностью описывает вашу программу и поможет пользователям и разработчикам понять, как работать с системой. Вы можете адаптировать его под свои конкретные нужды!
